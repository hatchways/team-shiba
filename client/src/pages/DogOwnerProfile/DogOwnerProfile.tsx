/* eslint-disable prettier/prettier */
import Grid from '@material-ui/core/Grid';

export default function DogOwnerProfile(): JSX.Element {
  return (
        <Grid>
            <div> This is the dog owner profile</div>
        </Grid>
    );
}